{
  "name": "cashflowapp",
  "version": "1.0.0",
  "description": "This document outlines the steps to set up a unified **Expense Management Application** using **JavaFX**, **Spring Boot**, **SQLite**, and **Tailwind CSS** for styling. The application features a dynamic UI welcome page as the entry point.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "npx tailwindcss -i ./src/main/resources/css/tailwind.css -o ./src/main/resources/css/output.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17"
  }
}
